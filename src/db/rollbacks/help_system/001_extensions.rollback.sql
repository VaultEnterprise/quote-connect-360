-- =========================================================
-- 001_extensions.rollback.sql
-- CAUTION: Do NOT run in shared environments without explicit approval.
-- Extensions may be used by other schemas/applications.
-- =========================================================

-- drop extension if exists pg_trgm;
-- drop extension if exists pgcrypto;